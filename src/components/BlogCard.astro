---
import { Image } from "astro:assets";

const { title, description, pubDate, url, image, tags } = Astro.props;
---

<article
  class="bg-surface rounded-2xl overflow-hidden shadow-md hover:shadow-xl transition-shadow duration-300 border border-border flex flex-col h-full"
>
  {
    image && (
      <div class="aspect-video w-full overflow-hidden">
        <Image
          src={image.src}
          alt={image.alt}
          width={600}
          height={315}
          class="object-cover w-full h-full hover:scale-105 transition-transform duration-300"
        />
      </div>
    )
  }

  <div class="p-6 flex flex-col flex-grow">
    {
      tags && (
        <div class="flex flex-wrap gap-2 mb-3">
          {tags.map((tag: string) => (
            <span class="text-xs font-medium px-2.5 py-0.5 rounded-full bg-pill text-pill-foreground">
              {tag}
            </span>
          ))}
        </div>
      )
    }

    <h3
      class="text-xl font-bold text-foreground mb-2 leading-tight"
    >
      <a href={url} class="hover:underline">{title}</a>
    </h3>

    <p
      class="text-muted text-sm mb-4 line-clamp-3 flex-grow"
    >
      {description}
    </p>

    <div
      class="mt-auto pt-4 border-t border-border flex items-center text-xs text-muted-subtle"
    >
      <time datetime={pubDate}>
        {
          new Date(pubDate).toLocaleDateString("en-US", {
            year: "numeric",
            month: "long",
            day: "numeric",
          })
        }
      </time>
    </div>
  </div>
</article>
